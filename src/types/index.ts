export interface Legend_T {
  name: string;
  active: boolean;
}

export interface Server_T {
  conn_date: string;
  conn_hours: number;
  id: string;
  max_user: number;
  server_ip: string;
  serverinfo_uid: string;
}
